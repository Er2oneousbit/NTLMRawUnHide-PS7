# NTLMRawUnHide.ps1 #

PowerShell 7 port of [NTLMRawUnhide.py](https://github.com/mlgualtieri/NTLMRawUnhide) by Mike Gualtieri.

**Original Author :** Mike Gualtieri
**Original Blog   :** https://www.mike-gualtieri.com
**Original Twitter :** https://twitter.com/mlgualtieri
**Original GitHub  :** https://github.com/mlgualtieri/NTLMRawUnhide
**Original Write up:** https://www.mike-gualtieri.com/posts/live-off-the-land-and-crack-the-ntlmssp-protocol

**PS7 Port Author  :** er2oneousbit

## Intro ##
NTLMRawUnHide.ps1 is a PowerShell 7 script that parses binary network packet capture files and extracts NTLMv2 hashes in a crackable format. It was ported from the original Python3 tool to run natively on Windows without a Python dependency. Like the original, it works directly with files generated by native Windows binaries like NETSH.EXE and PKTMON.EXE without conversion.

The following binary network packet capture formats are supported:
* *.pcap
* *.pcapng
* *.cap
* *.etl

## Requirements ##
* PowerShell 7+ (`pwsh.exe`) - will not run under Windows PowerShell 5.1

## Usage ##
.\NTLMRawUnHide.ps1 -i <inputfile> [-o <outputfile>] [-f] [-q] [-v]


Main options:
-f, -Follow                Continuously "follow" (e.g. "read from")
input file for new data
-i, -InputFile <file>      Binary packet data input file
(.pcap, .pcapng, .cap, .etl)
-o, -OutputFile <file>     Output file to append found NTLMv2 hashes
-q, -Quiet                 Only output found hashes; suppresses banner
and all status messages. Overrides -v.
-v, -VerboseOutput         Show field-level detail: offsets, lengths,
raw parsed values



## Examples ##

Extract NTLMv2 hashes from capture.pcapng:

.\NTLMRawUnHide.ps1 -i capture.pcapng
Same, but with verbose output:


.\NTLMRawUnHide.ps1 -i capture.pcapng -v
Extract hashes and continue monitoring the file for new data (like tail -f):


.\NTLMRawUnHide.ps1 -i capture.pcapng -f
Extract hashes and write them to a file:


.\NTLMRawUnHide.ps1 -i capture.pcapng -o hashes.txt
Quiet mode - hash output only, no banner (useful for piping):


.\NTLMRawUnHide.ps1 -i capture.pcapng -q
Packet Capture Methods
Any of the following methods can be used to create a compatible capture file:

Wireshark:

Set capture filter as "tcp port 445"; Save as .pcapng
tcpdump:

tcpdump -i eth0 -w capture.pcap "port 445"
NETSH.EXE:

netsh.exe trace start persistent=yes capture=yes TCP.AnyPort=445 tracefile=C:\Users\Public\capture.etl
netsh.exe trace stop
PKTMON.EXE:

pktmon.exe filter add SMB -p 445
:: List all filters
pktmon.exe filter list
:: Find id of the network adapter (example > Id: 9)
pktmon.exe comp list
:: pktmon.exe start --etw -p 0 -c [Adapter ID]
pktmon.exe start --etw -p 0 -c 9
:: Will create the file PktMon.etl in current directory
pktmon.exe stop
:: Cleanup
pktmon.exe filter remove
License
This project is licensed under the MIT License in accordance with the original tool.
See LICENSE for details.

References
The following resource was helpful in building the original tool and this port:

The NTLM Authentication Protocol and Security Support Provider
http://davenport.sourceforge.net/ntlm.html
